### HELLO WORLD

- .gitignore: determinadas pastas nao queremos subir, exemplo: conteudos restritos(ex: .env, .next) e pastas muito pesadas (node_modules)

- .nvmrc: define qual versao é requirida, utilizando nvm alias default (versao. EX:20)

- next.config.js: 

